import React from "react";

const UserContext = React.createContext(); // Creating the context from React library.

export default UserContext; // Exporting the context to use it in other components.

// Using the context we can create a provider (type of wrapper) and consumer component to pass the data globally in any component.
